# Work-study

## Back-end

Install dependencies:

```
cd back-end
npm install
```

Start server in development mode:

```
npm run start:watch
```

## Web app front-end (Create React App)

Install dependencies:

```
cd web-app
npm install
```

Start server in development mode:

```
npm start
```
